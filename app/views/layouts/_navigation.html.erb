<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <span>
                    <img src="<%= image_path('high-res.jpg') %>" width="100px" height="100px"class="img-responsive" style="margin: auto;">

                    </span>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">Goggligo Tech</strong>
                            </span> <span class="text-muted text-xs block"><%= current_user.full_name %> <b class="caret"></b></span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><%= link_to "Profile", user_path(current_user) %>
                        </li>
                        <li class="divider"></li>
                        <li>
                            <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
                        </li>
                    </ul>
                </div>
                <div class="logo-element">
                    
                    <img src="<%= image_path('high-res.jpg') %>" class="img-responsive">
                </div>
            </li>

            <li class="<%= is_active_controller('forms') %>">
                <a href="#"><i class="fa fa-user-md white"></i> <span class="nav-label white">Visitors</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('forms') %>">
                    <li class="<%= is_active_action('basic_forms') %>"><%= link_to "List", appointments_path, class: 'white' %></li>
                </ul>
            </li>

            <% if can? :manage, User %>
                <li class="<%= is_active_controller('forms') %>">
                    <a href="#"><i class="fa fa-users white"></i> <span class="nav-label white">Clients</span><span class="fa arrow"></span></a>
                    <ul class="nav nav-second-level collapse <%= is_active_controller('forms') %>">
                        <li class="<%= is_active_action('basic_forms') %>"><%= link_to "List", users_path, class: 'white' %></li>
                        <li class="<%= is_active_action('basic_forms') %>"><%= link_to 'New User', new_user_path, class: 'white' %></li>
                    </ul>
                </li>

            <% end %>

            <li class="<%= is_active_controller('forms') %>">
                <a href="#"><i class="fa fa-edit" style="color: white"></i> <span class="nav-label" style="color: white">Questions</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('forms') %>">
                    <li class="<%= is_active_action('basic_forms') %> white"><%= link_to "List", questions_path, class: 'white' %></li>
                    <% if can? :create, Question %>
                        <li class="<%= is_active_action('basic_forms') %> white"><%= link_to 'New Question', new_question_path, class: 'white' %></li>
                        <li class="<%= is_active_action('basic_forms') %> white"><%= link_to 'Template Question', questions_template_path, class: 'white' %></li>
                    <% end %>
                </ul>
            </li>

            <li class="<%= is_active_controller('forms') %>">
                <a href="#"><i class="fa fa-file" style="color: white"></i> <span class="nav-label" style="color: white">Reporting</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('forms') %>">
                    <li class="<%= is_active_action('basic_forms') %> white"><%= link_to 'Statistic', statistic_path, class: 'white' %></li>
                </ul>
            </li>
        </ul>
    </div>
</nav>

