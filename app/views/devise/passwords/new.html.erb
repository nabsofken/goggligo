<div class="loginColumns animated fadeInDown">
        <div class="row">

            <div class="col-md-6">
              <img src="<%= image_path('logo-transparent.svg') %>" width="300px" height="300px"class="img-responsive" >
            </div>

            <div class="col-md-6">
                <div class="ibox-content">
                <h2 class="font-bold">Welcome to Gliggo</h2>


<h2>Forgot/Set your password?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email, value: params[:email], autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Email", required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions", class: "btn btn-primary block full-width m-b" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>






                </div>
            </div>
        </div>
        <hr />
        <div class="row">
            <div class="col-md-6">
                Copyright Gliggo
            </div>
            <div class="col-md-6 text-right">
                <small>© 2019-2020</small>
            </div>
        </div>
    </div>





<%#= render "devise/shared/links" %>
